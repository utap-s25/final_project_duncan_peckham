<?xml version="1.0" encoding="utf-8"?>
<!--Generated by asking this to chatGPT: Can you generate an XML for me for an android app that is for a profile page. I want it to have the username at the top, then under that the email, then a section header saying "Favorite articles", then under that a recyclerview (which I will populate later)-->
<!-- Simple vertical layout for a profile page -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Username -->
    <TextView
        android:id="@+id/profileUsername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Username"
        android:textSize="18sp"
        android:textAppearance="?attr/textAppearanceHeadlineSmall"
        android:textStyle="bold" />

    <!-- Email -->
    <TextView
        android:id="@+id/profileEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="user@example.com"
        android:textSize="12sp"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:layout_marginTop="4dp" />

    <!-- Section header -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Log Out"
        android:id="@+id/logOutBut"
        />

    <TextView
        android:id="@+id/favoriteHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Past Votes"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_marginTop="24dp" />



    <!-- RecyclerView for favorite articles -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/matchupsRV"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:scrollbars="vertical" />

</LinearLayout>
